# Claude Code OpenAI API Configuration

# IMPORTANT: Path to Claude CLI (required for claude-code-sdk)
# The Docker container will have it at /usr/bin/claude
# For local development, adjust to your installation path
CLAUDE_CODE_PATH=/usr/bin/claude

# API Key for authentication
# Set this to secure your API endpoint
API_KEY=your-secure-api-key

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Server port (internal port is 8000, external can be changed in docker-compose.yml)
PORT=8000

# Claude Code SDK Configuration
# ============================

# Allowed tools for Claude to use
# Available: Bash, Read, Write, Edit, Grep, Glob, LS, WebSearch, etc.
# Default: Read, Grep, Glob, LS (safe read-only tools)
# CLAUDE_ALLOWED_TOOLS=Read,Grep,Glob,LS

# Maximum thinking tokens (default: 8000)
# CLAUDE_MAX_THINKING_TOKENS=12000

# Permission mode for tool usage
# - default: Interactive prompts (not suitable for API)
# - acceptEdits: Auto-accept file edits
# - plan: Plan mode - analyze without making changes
# - bypassPermissions: Allow all tools without prompts (API default)
# CLAUDE_PERMISSION_MODE=bypassPermissions

# Model Selection
# ==============

# Default model to use if not specified in request
# CLAUDE_DEFAULT_MODEL=claude-3-5-sonnet-20241022

# Model for simpler tasks (when using model size selection)
# CLAUDE_SMALL_MODEL=claude-3-5-haiku-20241022